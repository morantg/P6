{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <h1> Inscription</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    
    <button type="submit" class="btn-primary btn">Valider</button>

    {{ form_end(form) }}
</div>

{% endblock %}